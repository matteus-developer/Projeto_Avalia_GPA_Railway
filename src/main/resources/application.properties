    # application.properties
    spring.datasource.url=jdbc:mysql://root:WeMIxZXiZVcgzpriIeJfYrTjokGNnPNT@mysql.railway.internal:3306/railway
    spring.datasource.username=${MYSQLUSER}
    spring.datasource.password=${MYSQLPASSWORD}
    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
    
spring.jpa.hibernate.ddl-auto=update

# 1. Configuração que você já aplicou (e deve manter)
server.forward-headers-strategy=native

# 2. Configura o cookie de sessão para ser 'Secure' e só ser enviado por HTTPS.
# Isso resolve o problema de perda de sessão atrás do proxy reverso.
server.servlet.session.cookie.secure=true

# 3. Garante que o cookie de sessão seja acessível apenas por HTTP, 
# impedindo scripts (XSS) de roubá-lo. (Boa prática de segurança)
server.servlet.session.cookie.http-only=true



